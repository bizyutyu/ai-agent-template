# プロジェクトガイドライン

> このファイルはテンプレートです。`[ ]` で囲まれた箇所をプロジェクトに合わせて書き換えてください。
> 不要なセクションは削除して構いません。逆に、プロジェクト固有の規約が増えたら自由に追記してください。

## プロジェクト概要
- 目的: [このプロジェクトが解決する課題を1〜2文で]
- 技術スタック: [言語 / フレームワーク / 主要ライブラリ]
- ディレクトリ構成の要点: [重要なディレクトリとその役割]

## 開発規約
- コーディングスタイル: [Lint/Formatter設定への参照など]
- テスト方針: [テストフレームワーク、実行コマンド、カバレッジ方針]
- 型定義: 実装前に型/インターフェースを定義してから進める

## ワークフロー鉄則

### 探索 → 検証 → 安全
1. **探索**: 変更対象のコードとその依存関係を理解してから作業を始める
2. **検証**: 変更前に影響範囲(呼び出し元・テスト・設定)を確認する
3. **安全**: 破壊的操作(force push, DB操作, 削除系コマンド)は必ずユーザーに確認する

### ツール使用優先順位
1. `rg` (ripgrep) — 全文検索は `grep`/`find` より優先
2. `gh` — GitHub操作(PR/Issue/CI)はAPIを直接叩かずCLI経由
3. パッケージ実行は都度実行コマンド(`npx`/`uvx`等)を優先し、グローバルインストールは避ける

## コミット規約
[Conventional Commits](https://www.conventionalcommits.org/) に従う:
- `feat:` 新機能
- `fix:` バグ修正
- `docs:` ドキュメントのみの変更
- `style:` フォーマット等、動作に影響しない変更
- `refactor:` リファクタリング
- `test:` テストの追加・修正
- `chore:` その他(ビルド、依存関係更新など)

コミットメッセージ生成には `/commit` スキルを使う。

## セキュリティ注意事項
- `.env` など秘密情報を含むファイルは読み書きしない
- APIキー・トークン等の機密情報をコードやログに含めない
- 外部入力(ユーザー入力・API応答・ファイル内容)は常にバリデーションする
- 破壊的操作(force push, `rm -rf`, 本番DBへの直接操作など)は事前にユーザーへ確認する

## エージェント構成へのポインタ
このプロジェクトのAIエージェント設定は以下で管理する。詳細は各ディレクトリを参照:
- `.claude/settings.json` — 権限・環境変数・フックの登録
- `.claude/agents/` — サブエージェント定義
- `.claude/skills/` — スキル(スラッシュコマンド)定義
- `.claude/hooks/` — PreToolUse/PostToolUse等のフックスクリプト

構成の全体像は [README.md](./README.md) を参照。
